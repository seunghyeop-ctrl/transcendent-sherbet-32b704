# 최근 작업 정리

최신 기준 브랜치:
- `main`

최신 기준 커밋:
- `8fb2806` 이전 작업까지 원격 반영 완료

## 현재 상태

- GitHub 원격 저장소 연결 완료
- 첫 push 완료
- 이제 다른 맥/윈도우에서 `pull`로 이어서 작업 가능

## 최근 반영 내용

### 1. 배포 준비
- `Render` 기준 배포 파일 정리
- `Dockerfile` 보강
- `render.yaml` 보강
- `deploy/start.sh` 수정
- `Procfile` 수정
- `.env.example` 수정

### 2. 실행 경로 정리
- 배포 환경에서도 바로 실행되도록 시작 스크립트 정리
- `PROMPT_EXTRACTOR_HOST`, `PORT` 기반 실행 지원
- 컨테이너/서버 환경에서 사용할 앱 경로, 출력 경로 기본값 정리

### 3. 운영/동기화 문서
- `DEPLOY.md`
- `RENDER_QUICKSTART.md`
- `동기화가이드.md`

## 현재 원격 저장소

- `https://github.com/seunghyeop-ctrl/transcendent-sherbet-32b704.git`

## 회사 맥북프로에서 받는 방법

처음 한 번:

```bash
git clone https://github.com/seunghyeop-ctrl/transcendent-sherbet-32b704.git
cd transcendent-sherbet-32b704
```

이미 받아둔 상태면:

```bash
git pull
```

## 작업 내용 빠르게 보는 방법

```bash
git log --oneline -n 10
git show --stat HEAD
```

또는 이 문서를 열어서 현재 상태를 먼저 확인하면 됩니다.
